# Data Placeholders

This folder is intentionally empty except placeholders.

Planned paths:
- `data/raw/profiles.csv` for historical or forecast time-series.
- `data/processed/` for engineered features.

CSV schema is shown in `data/templates/profiles_template.csv`.

